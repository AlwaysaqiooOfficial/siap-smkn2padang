{
  "name": "siap-smkn2padang-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix server && npm run build --prefix server",
    "start": "npm run start --prefix server"
  }
}
